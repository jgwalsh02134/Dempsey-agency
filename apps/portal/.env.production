# Loaded automatically by `vite build` (production mode only).
# Not used by `vite` dev — dev still uses empty base + proxy unless VITE_API_BASE_URL is set locally.
VITE_API_BASE_URL=https://api.dempsey.agency
